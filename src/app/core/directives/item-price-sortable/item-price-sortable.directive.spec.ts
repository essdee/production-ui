import { ItemPriceSortableDirective } from './item-price-sortable.directive';

describe('ItemPriceSortableDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemPriceSortableDirective();
    expect(directive).toBeTruthy();
  });
});
